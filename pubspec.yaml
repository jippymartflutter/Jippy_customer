name: customer
description: "Jippy Mart Customer App"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 2.5.0+32

environment:
  sdk: '>=3.4.3 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  # google_play_integrity: ^1.0.0  # Removed as we're using platform channels

  cupertino_icons: ^1.0.8
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  firebase_storage: ^13.0.1
  firebase_messaging: ^16.0.1
  firebase_database: ^12.0.1
  firebase_crashlytics: ^5.0.1
  qr_code_dart_scan: ^0.11.0
#  carousel_slider: ^4.2.1

  country_code_picker: ^3.2.0
  crypto: ^3.0.6

  dotted_border: ^3.0.1
  geocoding: ^4.0.0
  googleapis_auth: ^2.0.0

  cached_network_image: ^3.4.1
  flutter_easyloading: ^3.0.5
  flutter_email_sender: ^7.0.0
  flutter_html: ^3.0.0-beta.2  # Use specific version instead of 'any'
  flutter_local_notifications: ^19.3.0
  flutter_svg: ^2.0.17
  geolocator: ^10.1.1  # Use specific version instead of 'any'
  location: ^8.0.1
  get: ^4.7.2
  google_maps_flutter: ^2.10.1
  google_sign_in: ^6.2.1
  image_picker: ^1.1.2
  intl: ^0.20.2
  pin_code_fields: ^8.0.1

  provider: ^6.1.2
  qr_flutter: ^4.1.0
  shared_preferences: ^2.5.2
  sign_in_with_apple: ^7.0.1
  syncfusion_flutter_datepicker: ^31.1.19
  timer_count_down: ^2.2.2
  url_launcher: ^6.3.1
  uuid: ^4.5.1
  package_info_plus: ^5.0.1
  date_picker_timeline: ^1.2.6
  flutter_rating_bar: ^4.0.1
  flutter_polyline_points: ^3.0.1
  clipboard: ^2.0.2
  map_launcher: ^4.4.2

  sqflite: ^2.4.2
  badges: ^3.1.2
  bottom_picker: ^4.1.1
  timelines_plus: ^1.0.6
  in_app_review: ^2.0.10
  share_plus: ^11.0.0
  mailer: ^6.4.1
  photo_view: ^0.15.0
  path_provider: ^2.1.5
  path: ^1.9.0
  video_compress: ^3.1.4
  video_player: ^2.9.3

  flutter_map: ^8.1.1
  latlong2: ^0.9.1

  #payment dependency

  #payment dependency
  # flutter_stripe: ^11.5.0
  razorpay_flutter: ^1.3.5  # ✅ FIXED: Downgraded to prevent NoSuchFieldError
  # flutter_paypal: ^0.2.1  # Commented out to reduce APK size

  google_maps_place_picker_mb: ^3.1.2

  # firebase_app_check: ^0.3.2+10  # TEMPORARILY DISABLED to eliminate SafetyNet dependencies
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  get_storage: ^2.1.1
  permission_handler: ^11.3.1
  
  # Smartlook for session recording and analytics
  flutter_smartlook: ^4.1.27
  
  # Deep link handling
  app_links: ^6.1.4
  font_awesome_flutter: ^10.9.1
  flutter_spinkit: ^5.2.2
  shimmer: ^3.0.0

dependency_overrides:
  webview_flutter: ^4.7.0
  google_api_headers: ^4.4.3
  package_info_plus: ^8.3.0
  location_platform_interface: ^4.0.0
  location_web: ^5.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  test:
  mockito: ^5.5.0
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  fonts:
    - family: Outfit
      fonts:
        - asset: assets/fonts/Outfit-Regular.ttf
        - asset: assets/fonts/Outfit-Medium.ttf
          weight: 500
        - asset: assets/fonts/Outfit-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Outfit-Bold.ttf
          weight: 700
        - asset: assets/fonts/Outfit-Light.ttf
          weight: 300
        - asset: assets/fonts/Outfit-Thin.ttf
          weight: 100
        - asset: assets/fonts/Outfit-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/Outfit-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/Outfit-Black.ttf
          weight: 900
    - family: Montserrat
      fonts:
        - asset: assets/fonts/Montserrat-Regular.ttf
        - asset: assets/fonts/Montserrat-Medium.ttf
          weight: 500
        - asset: assets/fonts/Montserrat-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Montserrat-Bold.ttf
          weight: 700
        - asset: assets/fonts/Montserrat-Light.ttf
          weight: 300
        - asset: assets/fonts/Montserrat-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/Montserrat-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/Montserrat-Black.ttf
          weight: 900
    - family: GoogleSansCode
      fonts:
        - asset: assets/fonts/GoogleSansCode.ttf




    # Removed duplicate font families to reduce APK size

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/fonts/
    - assets/videos/
    - assets/svg/
    - assets/
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section, like this:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
